[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "capcrack"
version = "0.1.0"
description = "A CTF-focused PCAP triage helper"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
capcrack = "capcrack.cli:main"

[tool.setuptools.packages.find]
where = ["src"]